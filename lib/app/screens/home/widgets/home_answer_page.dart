import 'package:flutter/material.dart';

class HomeAnswerPage extends StatelessWidget {
  const HomeAnswerPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
